# 管理员密码（必须修改）
ADMIN_PASSWORD=your-secure-password

# Session 密钥（可选，不设置会自动生成）
SECRET_KEY=your-random-secret-key

# 数据库路径（可选，默认 data.db）
DB_PATH=data.db

# 端口（可选，默认 5000）
PORT=5000

# 调试模式（可选）
DEBUG=false

# 自动同步间隔（秒，默认30）
SYNC_INTERVAL=30
